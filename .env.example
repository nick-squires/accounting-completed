DATABASE_URL=""
MAC_DB_SERVER=
MAC_DB_PORT=
MAC_DB_NAME=
MAC_DB_USER=
MAC_DB_PASSWORD=
MAC_DB_ENCRYPT=
# Set to "true" only for dev/local SQL Server instances with self-signed certs.
# Leave empty (default) for Azure SQL and any server with a publicly-trusted certificate.
MAC_DB_TRUST_CERT=
JWT_SECRET=
API_PORT=
